/* <--  IMPORT PACKAGE  --> */
const jwt = require('jsonwebtoken');

/*
* function generateToken
* @param user
* @param secrectSignature
* @param tokenLife 
*/

const generateToken = (user, secrectSignature, tokenLife) => {

}